# 🚀 Настройка Render.com - Пошаговая инструкция

## 📋 Обязательные поля

### 1. **Name** (Имя сервиса)
```
grant-arkhyz
```

### 2. **Environment** (Окружение)
```
Node
```

### 3. **Build Command**
```
npm install
```

### 4. **Start Command**
```
npm start
```

### 5. **Plan**
```
Free (или выберите платный план)
```

---

## ⚙️ Опциональные поля (можно оставить пустыми)

### **Secret Files** ❌ НЕ НУЖНО
- Оставьте пустым
- У нас нет секретных файлов (.env), все настройки в коде

### **Health Check Path** ✅ РЕКОМЕНДУЕТСЯ
```
/
```
или
```
/api/bookings
```
- Это поможет Render проверять, что сервер работает
- Можно оставить пустым, но лучше указать `/`

### **Pre-Deploy Command** ❌ НЕ НУЖНО
- Оставьте пустым
- У нас нет миграций базы данных или других предварительных команд

### **Auto-Deploy** ✅ ОСТАВЬТЕ ВКЛЮЧЕННЫМ
- ✅ **On Commit** - включено по умолчанию
- Это означает, что при каждом коммите в GitHub будет автоматический деплой

### **Build Filters** ❌ НЕ НУЖНО
- Оставьте пустым
- Render будет деплоить при любых изменениях

---

## 🔧 Переменные окружения (Environment Variables)

В разделе **Environment** добавьте (если нужно):

| Key | Value | Обязательно |
|-----|-------|-------------|
| `NODE_ENV` | `production` | Нет (но рекомендуется) |
| `PORT` | `10000` | Нет (Render установит автоматически) |

**Примечание:** В нашем `server.js` уже используется `process.env.PORT || 3000`, поэтому Render автоматически установит правильный порт.

---

## 📝 Пошаговая инструкция

1. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите репозиторий `mariamaksikova/grant-arkhyz`

2. **Заполните основные поля:**
   - **Name:** `grant-arkhyz`
   - **Environment:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** `Free`

3. **Health Check Path (опционально):**
   - Введите: `/`

4. **Остальные поля:**
   - **Secret Files:** оставьте пустым
   - **Pre-Deploy Command:** оставьте пустым
   - **Auto-Deploy:** оставьте включенным ✅
   - **Build Filters:** оставьте пустым

5. **Нажмите "Create Web Service"**

6. **Дождитесь деплоя:**
   - Render автоматически установит зависимости
   - Запустит сервер
   - Вы получите URL вида: `https://grant-arkhyz.onrender.com`

---

## ✅ Проверка после деплоя

1. **Откройте главную страницу:**
   ```
   https://your-app.onrender.com
   ```

2. **Проверьте админку:**
   ```
   https://your-app.onrender.com/admin.html
   ```

3. **Проверьте API:**
   ```
   https://your-app.onrender.com/api/bookings
   ```

---

## 🐛 Если что-то не работает

1. **Проверьте логи:**
   - В панели Render нажмите "Logs"
   - Ищите ошибки

2. **Проверьте Build Logs:**
   - Убедитесь, что `npm install` прошел успешно

3. **Проверьте Runtime Logs:**
   - Убедитесь, что сервер запустился
   - Должно быть: "Сервер запущен на http://localhost:XXXX"

---

## 💡 Советы

- **Бесплатный план:** Сервис "засыпает" после 15 минут бездействия. Первый запрос может занять 30-60 секунд для "пробуждения".
- **База данных:** SQLite файл сохраняется на диске. На бесплатном плане это работает нормально.
- **Автоматический деплой:** При каждом push в GitHub будет автоматический деплой.

---

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте логи в Render
2. Убедитесь, что все поля заполнены правильно
3. Проверьте, что репозиторий подключен корректно

