# Инструкция по деплою сайта Grant Arkhyz

## 🚀 Варианты хостинга

### 1. Render.com (Рекомендуется - бесплатный план)

**Преимущества:**
- Бесплатный план с автоматическим деплоем
- Поддержка SQLite
- Автоматические SSL сертификаты
- Простая настройка

**Шаги:**

1. **Создайте аккаунт на [Render.com](https://render.com)**

2. **Подготовьте проект:**
   ```bash
   # Убедитесь, что все файлы закоммичены
   git add .
   git commit -m "Prepare for deployment"
   ```

3. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Настройки:
     - **Name:** grant-arkhyz
     - **Environment:** Node
     - **Build Command:** `npm install`
     - **Start Command:** `npm start`
     - **Plan:** Free

4. **Переменные окружения:**
   - `NODE_ENV` = `production`
   - `PORT` = `10000` (Render автоматически установит свой порт)

5. **Деплой:**
   - Render автоматически задеплоит проект
   - После деплоя вы получите URL вида: `https://grant-arkhyz.onrender.com`

6. **Доступ к админке:**
   - `https://your-app.onrender.com/admin.html`

---

### 2. Railway.app

**Преимущества:**
- Очень простой деплой
- Автоматический деплой из GitHub
- Бесплатный план с ограничениями

**Шаги:**

1. **Создайте аккаунт на [Railway.app](https://railway.app)**

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Railway автоматически определит настройки:**
   - Использует `Procfile` для запуска
   - Автоматически установит зависимости

4. **Переменные окружения (опционально):**
   - `NODE_ENV` = `production`

5. **Деплой:**
   - Railway автоматически задеплоит проект
   - Получите URL вида: `https://grant-arkhyz.up.railway.app`

---

### 3. Heroku

**Преимущества:**
- Надежный хостинг
- Много документации

**Шаги:**

1. **Установите Heroku CLI:**
   ```bash
   # macOS
   brew tap heroku/brew && brew install heroku
   
   # Или скачайте с https://devcenter.heroku.com/articles/heroku-cli
   ```

2. **Войдите в Heroku:**
   ```bash
   heroku login
   ```

3. **Создайте приложение:**
   ```bash
   heroku create grant-arkhyz
   ```

4. **Деплой:**
   ```bash
   git push heroku main
   ```

5. **Откройте приложение:**
   ```bash
   heroku open
   ```

---

### 4. Vercel (Только для статики + API Routes)

**⚠️ Внимание:** Vercel лучше подходит для статических сайтов. Для полноценного Node.js приложения лучше использовать Render или Railway.

**Шаги:**

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Деплой:**
   ```bash
   vercel
   ```

3. **Следуйте инструкциям в терминале**

---

## 📋 Важные моменты

### База данных

SQLite будет создаваться автоматически при первом запуске. На бесплатных планах:
- **Render:** База данных сохраняется на диске (1GB бесплатно)
- **Railway:** База данных сохраняется, но может быть удалена при бездействии
- **Heroku:** Используйте PostgreSQL (требует платный план для постоянного хранения)

### Переменные окружения

Убедитесь, что установлены:
```bash
NODE_ENV=production
PORT=10000  # или порт, который предоставляет хостинг
```

### Проверка после деплоя

1. **Откройте главную страницу:**
   - Должна загрузиться без ошибок

2. **Проверьте формы:**
   - Заполните тестовую форму
   - Проверьте, что данные сохраняются

3. **Проверьте админку:**
   - Откройте `/admin.html`
   - Убедитесь, что данные загружаются
   - Проверьте автообновление (должно обновляться каждые 5 секунд)

4. **Проверьте API:**
   - `https://your-app.com/api/bookings`
   - `https://your-app.com/api/reviews`
   - `https://your-app.com/api/investments`

---

## 🔧 Локальная проверка перед деплоем

```bash
# 1. Установите зависимости
npm install

# 2. Запустите сервер
npm start

# 3. Проверьте в браузере
# http://localhost:3000

# 4. Проверьте админку
# http://localhost:3000/admin.html

# 5. Проверьте API
# http://localhost:3000/api/bookings
```

---

## 🐛 Решение проблем

### Проблема: "Cannot find module"

**Решение:**
```bash
# Убедитесь, что package.json содержит все зависимости
npm install

# Проверьте, что node_modules не в .gitignore (но он должен быть)
# Зависимости должны устанавливаться на сервере
```

### Проблема: "Port already in use"

**Решение:**
- На хостинге порт устанавливается автоматически через переменную `PORT`
- Убедитесь, что в `server.js` используется `process.env.PORT || 3000`

### Проблема: "Database locked"

**Решение:**
- SQLite может блокироваться при множественных запросах
- На продакшене рассмотрите переход на PostgreSQL

### Проблема: Формы не работают

**Решение:**
1. Убедитесь, что сайт открыт через HTTPS (не file://)
2. Проверьте консоль браузера (F12)
3. Проверьте логи сервера на хостинге
4. Убедитесь, что CORS настроен правильно

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи на хостинге
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все файлы закоммичены в Git
4. Проверьте переменные окружения

---

## ✅ Чеклист перед деплоем

- [ ] Все файлы закоммичены в Git
- [ ] `.gitignore` настроен правильно
- [ ] `package.json` содержит все зависимости
- [ ] `server.js` использует `process.env.PORT`
- [ ] Локально все работает
- [ ] Формы сохраняют данные
- [ ] Админка загружает данные
- [ ] Автообновление админки работает

---

## 🎉 После успешного деплоя

1. Сохраните URL вашего сайта
2. Добавьте его в закладки
3. Настройте кастомный домен (если нужно)
4. Настройте мониторинг (опционально)

